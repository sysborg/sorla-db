const chai = require('chai')
const expect = chai.expect
const faker = require('faker')
const sorla = require('../src/sorla.js')

describe('SORLA Collection Unit Text', () => {
    it('Should insert a document in collection', () => {
        expect(1).to.equal(1);
    });
});